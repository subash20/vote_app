apiVersion: v1
kind: Pod
metadata:
  name: votting-app-pod
  labels:
    name: votting-app-pod
    app: demo-votting-app
spec:
  containers:
    - name: votting-app
      image: dockersamples/examplevotingapp_vote
      ports:
        - containerPort: 80
